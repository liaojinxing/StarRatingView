language: objective-c
before_install:
  - brew update
  - brew uninstall xctool
  - brew install xctool
  - export LANG=en_US.UTF-8
  - gem i cocoapods --no-ri --no-rdoc
  - pod install
xcode_workspace: StarRatingView.xcworkspace
xcode_scheme: StarRatingView
xcode_sdk: iphonesimulator

